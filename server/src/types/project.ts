export type ProjectBody = {
    title: string
    description: string
}

export type ProjectType = {
    title: string
    description: string
    user_id: string
    id: string
    createdAt: string
    updatedAt: string
}